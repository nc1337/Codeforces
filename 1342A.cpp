#include <bits/stdc++.h>

using namespace std;

int main() {
  int t;
  cin>>t;
  while(t--) {
    long double sum = 0;
    long double x,y,a,b;
    long double p;
    cin>>x>>y;
    cin>>a>>b;
    b = (2*a < b) ? (2*a): b;
    if (2 * a <= b) {
      printf("%.LF\n", (x+y)*a);
    } else {
      printf("%.LF\n", ())
    }
 
  return 0;
}
